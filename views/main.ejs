<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Movies&Comics</title>
  <script src="https://code.jquery.com/jquery-3.1.1.min.js" type="text/javascript"></script>
  <link rel="stylesheet" href="">
</head>
  <body>
  <a href="http://localhost:3000/">
    <input type="button" value="Log Out"/>
  </a>
  <a href="http://localhost:3000/profile">
    <input type="button" value="Profile"/>
  </a>
  </br>
  <form action="/searched" method="get">
    <input type="text" name="mpfour" placeholder="Search Movies or Show">
    <input type="submit" value="Search">
  </form>
  <select id="dropdown">
    <option value="movie">movie</option>
    <option value="tv">tv</option>
  </select>
  <h1>Movies</h1>
    <div>
    <% if (comic.length) { %>
      <h1><%= movie.title %></h1>
      <img src="https://image.tmdb.org/t/p/w300_and_h450_bestv2<%= movie.poster_path %>" >
      <p><%= movie.overview %></p>
    <% }; %>
    </div>
  <h1>Comics</h1>
  <% if (comic.length >= 2) { %>
    <% for (let i = 0; i < 5; i++) { %>
      <h1><%= comic[i].title %></h1>
      <img src="<%= comic[i].images[0].path %>.jpg" alt="">
      <p><%= comic[i].description %></p>
        <form action="/profile" method="post">
          <input type="hidden" name="saved[title]" value="<%= comic[i].title %>" >
          <input type="hidden" name="saved[image]" value="<%= comic[i].images[0].path %>.jpg" >
          <input type="hidden" name="saved[description]" value="<%= comic[i].description %>">
          <input type="submit" value="Save">
        </form>
      <% }; %>
    <% }; %>
  </div>
  </body>
</html>
